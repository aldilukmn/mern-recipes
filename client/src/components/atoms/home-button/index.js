import React from "react";
import { Link } from "react-router-dom";

const HomeButton = () => {
  return (
    <>
      <a className="nav-link active" style={{ cursor: "pointer" }}>Home</a>
    </>
  );
};

export default HomeButton;
